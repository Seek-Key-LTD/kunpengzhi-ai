"""📖 记忆银行 — 叙事主线"""

import streamlit as st

st.set_page_config(page_title="记忆银行 · 叙事", page_icon="📖", layout="wide")

st.markdown("# 📖 记忆银行 · 从文明到记忆")

with st.expander("📜 第一章：人类文明与智能体", expanded=True):
    st.markdown("""
    ### 四本书 · 一条主线

    从 2024 年开始，围绕 **"人类文明 × 人工智能"** 这个母题，逐步完成四部著作：

    | 著作 | 主题 | 核心观点 |
    |------|------|----------|
    | **《文明的重构》** | 人类文明的底层逻辑 | 信息—能量—意识的三角模型 |
    | **《智能体的觉醒》** | AI 作为文明新物种 | 从工具到协作者的范式转移 |
    | **《记忆的边界》** | 集体记忆与个体认知 | 记忆是智能的基础设施 |
    | **《深度发布》** | AI 时代的出版哲学 | 链上确权 + 开放共享 |
    """)

with st.expander("🧠 第二章：Memory Bus 四层管线", expanded=True):
    st.markdown("""
    ```
    ┌─────────────────────────────────────────────────────┐
    │                 记忆银行 · Memory Bus                 │
    ├──────────┬──────────┬──────────┬────────────────────┤
    │  📡 采集层  │  🧠 图谱层 │  🧩 演绎层 │  🔗 链上确权     │
    │  mem0     │  Neo4j   │  gbrain  │  Base Sepolia     │
    │  7 Agent  │  知识图谱 │  深度推理 │  每份记忆哈希存证    │
    │  无差别归集 │  关系挖掘  │  语义推演  │  智能合约验真      │
    └──────────┴──────────┴──────────┴────────────────────┘
    ```

    #### 第一层：📡 无差别采集（mem0）
    - 7 个智能体每小时对话自动归集
    - 不分轻重、不设筛选——所有的思考痕迹都是记忆的原材料

    #### 第二层：🧠 知识图谱（Neo4j）
    - 从对话中提取实体、关系和事件
    - 构建智能体之间的知识网络

    #### 第三层：🧩 深度演绎（gbrain）
    - 基于图谱的语义推理
    - 从已知事实推导隐含结论

    #### 第四层：🔗 链上确权
    - 每份记忆指纹写入 Base Sepolia
    - 智能合约开放查询——不可篡改 · 永久存证
    """)

with st.expander("⚙️ 第三章：技术实现", expanded=False):
    st.markdown("""
    ```
    Agent-Collect → MongoDB → OnBar(Cron) → 记忆银行
                                           ├── mem0 (异步推送)
                                           ├── Neo4j (知识图谱)
                                           ├── gbrain (深度演绎)
                                           └── DEX (链上哈希)
    ```
    - **无差别归集**：所有 agent 一视同仁
    - **异步解耦**：mem0 异步推送，不阻塞主流程
    - **MinIO 排除**：直接从 MongoDB 读取
    """)

with st.expander("🔮 第四章：未来展望", expanded=False):
    st.markdown("""
    - **Phase 2**：跨节点同步
    - **Phase 3**：记忆继承
    - **Phase 4**：自主遗忘
    - **Phase 5**：文明存档
    """)

st.divider()
st.markdown("← [返回首页](/)")
